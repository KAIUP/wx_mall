.tabs-title {
  display: flex;
}
.tabs-title .title-item {
  flex: 1;
  text-align: center;
}
.tabs-title .title-item text {
  display: inline-block;
  padding: 10rpx 15rpx;
}
.tabs-title .title-item .active {
  color: var(--themeColor);
  border-bottom: 5rpx solid currentColor;
}
